import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text: {
      color: 'blue',
      fontWeight: 'bold',
      fontSize: 30,
    },
    container: {
      paddingTop:64,
    },
  });

export default styles;